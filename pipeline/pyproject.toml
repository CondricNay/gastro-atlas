[project]
name = "pipeline"
version = "0.1.0"
description = "LLM extraction pipeline for Gastro Atlas"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.6.2",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]
